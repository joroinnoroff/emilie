import { work } from "./work"
import { series } from "./series"
import { siteSettings } from "./siteSettings"
import { about } from "./about"
import { cvItem } from "./cvItem"

export const schemaTypes = [work, series, siteSettings, about, cvItem]
